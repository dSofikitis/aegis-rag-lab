# Security Notes

Principle of least privilege limits access to the minimum permissions required to do a job.
Zero trust assumes no implicit trust and continuously verifies identity and device posture.
Defense in depth layers controls so a single failure does not expose critical assets.
Security logging should capture authentication events, privilege changes, and policy decisions.
